[tox]
envlist = all
skipsdist = True

[testenv]
setenv =
   VIRTUAL_ENV={envdir}
deps = -r{toxinidir}/requirements.txt
commands = sh {toxinidir}/tools/generate_ssh_config.sh {toxinidir}
whitelist_externals = sh
sitepackages=True
envdir={toxworkdir}/kubic-testinfra

[testenv:admin]
commands = 
    {[testenv]commands}
    pytest --ssh-config={env:SSH_CONFIG:{toxinidir}/tools/ssh_config} --sudo -m "admin or common" {posargs} --junit-xml admin.xml -v

[testenv:master]
commands = 
    {[testenv]commands}
    pytest --ssh-config={env:SSH_CONFIG:{toxinidir}/tools/ssh_config} --sudo -m "master or common" {posargs} --junit-xml master.xml -v

[testenv:worker]
commands = 
    {[testenv]commands}
    pytest --ssh-config={env:SSH_CONFIG:{toxinidir}/tools/ssh_config} --sudo -m "worker or common" {posargs} --junit-xml worker.xml -v

[testenv:all]
commands = 
    {[testenv]commands}
    sh {toxinidir}/tools/testAllRoles.sh {env:ENVIRONMENT_JSON:{toxinidir}/tools/environment.json} {env:SSH_CONFIG:{toxinidir}/tools/ssh_config} {posargs}

[testenv:linters]
deps = -r{toxinidir}/test-requirements.txt
commands = sh tools/pretty_flake8.sh
envdir={toxworkdir}/{envname}

[flake8]
ignore = D203
exclude =
    .git,
    __pycache__,
    build,
    .venv,
    .tox